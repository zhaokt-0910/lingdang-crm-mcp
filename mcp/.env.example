# 灵当CRM MCP Server v4 配置（零依赖版）
# 用户名+密码登录认证（不需要 API 授权码，客户一般无需配置本文件）
# 环境变量优先级：连接器 mcp.json 的 env > 本文件 > 代码默认值

# CRM 访问地址（末尾不要带 /）—— 客户通常不配，改用 setup_instance 注册账套
CRM_BASE_URL=

# HTTP 超时（秒）
CRM_HTTP_TIMEOUT=15.0
